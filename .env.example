# Database
DATABASE_HOST=localhost
DATABASE_USER=root
DATABASE_PASSWORD=
DATABASE=thrifts

# JWT
# Generate JWT_SECRET dengan: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_IN=7d
JWT_COOKIE_EXPIRES=7

# Server
PORT=5000
NODE_ENV=development
