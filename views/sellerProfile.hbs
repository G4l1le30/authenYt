<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{profile_user.name}}'s Profile - Fashion HUB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/style.css" rel="stylesheet">
</head>
<body>
    {{> navbar_main }} <div class="container mt-5 pt-4"> <div class="card mb-4">
            <div class="card-header">
                <h3>{{profile_user.name}}'s Profile</h3>
            </div>
            <div class="card-body">
                <p><strong>Name:</strong> {{profile_user.name}}</p>
                </div>
        </div>

        <div class="card mb-4" id="seller-products">
            <div class="card-header"><h4>Products by {{profile_user.name}}</h4></div>
            <div class="card-body">
                {{#if seller_products.length}}
                <div class="row g-3">
                    {{#each seller_products}}
                    <div class="col-lg-3 col-md-4 col-sm-6">
                        <div class="product-card-new custom-card h-100">
                            <a href="/product/{{this.id}}" class="product-image-link">
                                <img src="{{this.image_url}}" class="card-img-top product-card-img" alt="{{this.name}}">
                            </a>
                            <div class="card-body text-center product-card-body">
                                <h5 class="product-name mb-2">{{this.name}}</h5>
                                <div class="product-rating mb-2">
                                    {{{generateStars this.rating}}}
                                </div>
                                <p class="product-price fw-bold mb-0">Rp {{this.price}}</p>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
                {{else}}
                <p>{{profile_user.name}} has no products listed for sale.</p>
                {{/if}}
            </div>
        </div>
    </div>

    {{> footer_main }} <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>